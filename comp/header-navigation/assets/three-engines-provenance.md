# Legacy Discover / Convert / Scale asset provenance

Status: retained as unreferenced design-history artifacts. The live section no longer loads these files; its three product scenes are rendered entirely with semantic HTML and CSS.

These three shipping PNGs are section-specific crops derived from the user-approved ImageGen comp at `comp/three-engines/renders/three-engines-approved.png`:

- `three-engines-discover.png`: lead discovery and qualification scene.
- `three-engines-convert.png`: omnichannel conversation and deal handoff scene.
- `three-engines-scale.png`: AI insight, forecast, and committed add-ons scene.

The Scale crop was refined after review to remove speculative integrations and now names only committed RakanSales capabilities supplied in the project brief: AI Voice Agent, AI Lead Finder, and LinkedIn Enrichment.

Each PNG also stores `Source` and `Prompt` text metadata. These assets are illustrative product storytelling rather than literal production screenshots; the page exposes that status in live text above every scene.
